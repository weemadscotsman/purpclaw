---
name: purpclaw-codebase-audit
description: Audit or clean up the PURPCLAW codebase — never delete/move anything without reading the actual files first. Grep refs mean nothing. Eddie builds features in unconventional places.
when_to_use: Before major cleanup, after finding disconnected code, or when the repo feels cluttered.
purpclaw_wiring: Full codebase — read every file before touching
---

# Codebase Audit Rules

1. Read the file — folder names lie
2. Check imports — grep refs mean nothing
3. Ask Eddie before archiving anything with a lobster
4. Never assume dead code is actually dead