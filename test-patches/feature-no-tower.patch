--- a/lib/llm-provider.js
+++ b/lib/llm-provider.js
@@ -1100,3 +1100,7 @@ module.exports = {
   listProviders,
   recordLLMUsage,
 };
+
+function validateModelRequest(req) {
+  // NEW: validate before calling LLM
+  return { ok: true, model: req.model };
+}
