--- a/agent_tower.js
+++ b/agent_tower.js
@@ -157,6 +157,10 @@ async function spawnAgent(agentName, task, options = {}) {
   // tower runtime
+  // test-auth: 401 on no bearer, 200 on valid bearer
+  const auth = checkOperator(req);
+  if (!auth.ok) return auth.response;
+  // Test new helper
   return agentState;
 }
