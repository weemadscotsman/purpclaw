# Archive — Legacy/Reference Only

This folder contains old snapshots and legacy code.

**Do not run `npm install`, `pnpm install`, or `yarn install` here.**
Do not commit `node_modules`.
This folder is not an install target or workspace member.
