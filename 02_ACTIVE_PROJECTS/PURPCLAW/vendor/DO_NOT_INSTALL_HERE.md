# Vendor — External Copied Code

This folder contains third-party code copied into the repo.

**Do not run `npm install`, `pnpm install`, or `yarn install` here.**
Do not modify vendor code unless explicitly approved.
Do not add vendor folders to workspace or install targets.
