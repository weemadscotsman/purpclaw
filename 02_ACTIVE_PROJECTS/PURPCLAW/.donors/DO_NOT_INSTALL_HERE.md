# Donor App — Source/Reference Only

This folder is donor/prototype material.

**Do not run `npm install`, `pnpm install`, or `yarn install` here.**
Do not commit `node_modules`.
Do not add this folder as a workspace or install target.

Extract useful components/capabilities into `apps/*` or `packages/*` after audit.
Keep only source/config/public — no dependency artifacts.
