# LOOP.md

> Generated by OMNI-SURGEON Phase Four on 2026-06-13T15:48:57.645Z.
> Schema 0.1.0-phase-four.

## Standing debug-forever loop

```
audit     → run lib/omni/truth-scanner.js
cross-check → run lib/omni/feature-registry.js
plan      → pick a single actionRequired feature
repair    → small, reversible, one finding
execute   → touch only the files in the plan
verify    → smoke test, curl, or service restart
document  → update STRESS/<cycle>.md
repeat    → next actionRequired feature
```

## Action queue (from feature-registry.json)

These are the next cycle targets. Pick ONE per cycle.

- **bridge** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **cockpit** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **dash** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **inline** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **harness** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **mochi** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **pipeline** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **preprompt** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **skyscraper** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **voice** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **harness** — state: `missing-wiring` — page exists but no route, agent, or same-name component
- **OBLITERATUS** — state: `partial` — canned OBLITERATUS routes still in unified_api; pre-prompt compiler is the real command-law layer (different name)
- **api-mega-list** — state: `partial` — POST is intentionally 403 (use GOOP broker); operator must decide if read-only is right or wire write path
- **GOOP** — state: `partial` — broker/registry for API entries; routes exist; needs operator wiring decision
- **Kimi** — state: `planned` — Kimi K2 swarm provider; configured in .env; no UI consumer
- **Shaman** — state: `planned` — no routes or UI detected; needs purpose investigation
- **Security** — state: `partial` — security routes exist; may be empty status stubs
- **Sessions** — state: `planned` — session routes defined; not wired to real operator/session state
- **Gestures** — state: `planned` — gesture routes; needs purpose investigation
- **Mochi** — state: `partial` — page works; some UI elements may show canned state; verify against /api/mochi
- **Voice** — state: `failing` — voice-coordinator service was down per /api/services probe; needs voice diagnostic chain
- **Research** — state: `partial` — route proxies to orchestrator; verify that orchestrator /api/swarm/research is real
- **Narrator** — state: `partial` — 14 event types narrated have no backend producer; needs publishers added
- **Hooks** — state: `partial` — 6 hook polls to non-existent routes; needs routes created or hooks re-pointed

## Cycle history

| # | Date | Goal | Status | Notes |
|---|------|------|--------|-------|

## Pending operator decisions

- `bridge` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `cockpit` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `dash` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `inline` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `harness` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `mochi` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `pipeline` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `preprompt` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `skyscraper` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `voice` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `harness` (missing-wiring) — operator must decide: wire it, defer it, or operator-disable it explicitly
- `Kimi` (planned) — Kimi K2 swarm provider; configured in .env; no UI consumer
- `Shaman` (planned) — no routes or UI detected; needs purpose investigation
- `Sessions` (planned) — session routes defined; not wired to real operator/session state
- `Gestures` (planned) — gesture routes; needs purpose investigation
- `Voice` (failing) — voice-coordinator service was down per /api/services probe; needs voice diagnostic chain

## Files the next cycle must read

- STRESS/AUDIT-MASTER.md
- STRESS/OMNI-SURGEON-PHASE-ONE.md
- STRESS/OMNI-SURGEON-PHASE-TWO.md
- agent_work/omni/truth-snapshot.json
- agent_work/omni/feature-registry.json
- agent_work/omni/last-patch-review.json (if exists)
