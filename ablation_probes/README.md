# Research Only

This directory contains ablation experiments.

These are **not loaded by the PURPCLAW runtime**.
They are isolated experiments run manually against specific model checkpoints.

Purpose: refusal weight tuning on 7B models.
Not wired to any CLI command or agent.
